# System Architecture

This project implements a privacy-preserving federated learning system
for energy demand forecasting under non-IID data distributions.

Clients train local models on private time-series data.
Only model updates are shared with the server.
